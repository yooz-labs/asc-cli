"""Integration tests that require real API credentials.

These tests are skipped in CI and should be run locally with:
    pytest -m integration

Ensure you have valid credentials configured:
    asc auth login
"""
